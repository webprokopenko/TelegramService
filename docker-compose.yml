version: '3'
services:

    telegram:
      build:
        context: ./
        dockerfile: ./Dockerfile
      user: "node"
      working_dir: /home/node/app
      environment:
        - NODE_ENV=production
      volumes:
        - ./src:/home/node/app
      expose:
        - "8082"
      ports:
        - 8082:8082
      command: "npm start"